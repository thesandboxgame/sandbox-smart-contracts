import {setupPredicate} from './fixture';
// import {smockit, smoddit} from '@eth-optimism/smock';

describe('Matic: ERC1155 Predicate', function () {
  it('does something', async function () {});
  it('should fail if...', async function () {
    const {erc1155Predicate} = await setupPredicate();
  });
});
