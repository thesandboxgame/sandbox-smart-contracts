// const {assert} = require("chai-local");
const {setupLandSale} = require("./fixtures");

describe("testLandSaleWithReferral", function () {
  it("intro", async function () {
    // const contracts = await setupLandSale();
    // console.log(contracts);
  });
});
